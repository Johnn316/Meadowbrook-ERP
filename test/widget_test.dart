import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder — full widget tests will be added once mock providers
  // are wired up for the ERP screens.
  test('placeholder', () {
    expect(true, isTrue);
  });
}
